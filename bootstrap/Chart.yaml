apiVersion: v2
name: bootstrap
description: Un chart Helm paraguas para desplegar la aplicacion WellTrack App of Apps

# Tipo de chart. 'application' es el tipo mas comun.
type: application

# Version del chart. Sigue las convenciones de SemVer.
version: 0.1.0

# Version de la aplicacion que despliega este chart.
appVersion: "1.0"

# (Opcional) Dependencias de otros charts
dependencies: [] 